# TODO Progress Tracker

## Prisma Setup ✅
- Schema fixado ✅
- Client gerado ✅
- DB up (docker compose up database -d) ✅
- .env created ✅
- `npx prisma db push` ✅

## Fix for prisma.config.ts Error ✅
- Removed prisma.config.ts
- Used --url flag for db push ✅

## Next Steps
- Run `npm run dev` to test app
- Add authentication (NextAuth)
- Implement CRUD for models (User, Post, etc.)
- Deploy to Vercel


